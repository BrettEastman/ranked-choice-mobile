// Spacing scale
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;

// App limits
export const limits = {
  minCandidates: 2,
  maxCandidates: 20,
  minVoters: 2,
  maxVoters: 50,
  defaultMaxRankChoices: 3,
  shareCodeLength: 6,
} as const;
